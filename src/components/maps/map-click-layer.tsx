"use client";

import { useMapEvents } from "react-leaflet";

export function MapClickLayer({ onMove }: { onMove: (lat: number, lng: number) => void }) {
  useMapEvents({
    click(e) {
      onMove(e.latlng.lat, e.latlng.lng);
    },
  });
  return null;
}
